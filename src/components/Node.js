import React from "react"
import {NODE_SIZE} from "../config/constants.js"

export default ({

}) => {
    return(
        <div className="node" style={{width: NODE_SIZE, height: NODE_SIZE}}/>
    )
}
